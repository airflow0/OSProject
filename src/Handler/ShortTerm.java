package Handler;
import KOS.ProcessControlVariables;
public class ShortTerm
{
    // Used to store LongTermSchedulerData onto the PCB and remove them as CPU finishes process.
    //public ProcessControlVariables removeAndGet( int item)
    {
        //create another processControlVariable and store processcontroble variable .get( item )
        // remove using .remove from arraylist functions longterm.controller.remove(i);
        //return the temporary processcontrolvariable.
    }
    /**
     * sorting
     * must have boolean operation to check if
     * iterate for size of queue in longtermscheduler
     * if longterm.get(iteration).getProcPriority() < longterm.get(iteration + 1) then
     * create new processcontrolvariable set it to longt.get(i)
     * swap
     * continue checking.
     */
}
